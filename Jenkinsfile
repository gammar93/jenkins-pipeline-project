pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo "Last versin of Jenkins from git auto push v1"
                sh 'echo using shell within Jenkinsfile'
                echo 'not using shell in the Jenkinsfile'
            }
        }
    }
}